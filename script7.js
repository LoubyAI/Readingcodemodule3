//level7 try to change the value of "number" by erasing 10 and writing a different number and observe the different output
let number = 7;
if (number > 5) {
    document.getElementById("message").innerHTML = "Number is greater than 5";
} else {
    document.getElementById("message").innerHTML = "Number is 5 or less";
}

//Instead of the text where the html code has the id="message" is in
// The text "number is greater than 5" will appear 
// but if the number is less then 5 "Number is 5 or less" will appear.